<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @part.name %>
</p>

<p>
  <strong>Introduction:</strong>
  <%= @part.introduction %>
</p>

<%= link_to 'log詳細', car_part_log_path(@car, @part) %>
<%= link_to 'Edit', edit_car_part_path(:car_id => @car.id, :id => @part.id) %> |
<%= link_to 'Back', car_path(@car.id) %>

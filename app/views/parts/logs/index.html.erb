<p id="notice"><%= notice %></p>

<h1>履歴</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>


  <tbody>
    <% @logs.each do |log| %>
      <tr>
      <td><%= log.id %></td>
      <td><%= log.part %></td>
      <td><%=  %></td>
        <td><%= link_to 'Show', car_log_path(@car, log) %></td>
        <td><%= link_to 'Edit', edit_car_log_path(@car, log) %></td>
        <td><%= link_to 'Destroy', car_log_path(@car, log), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '履歴作成', new_car_log_path(@car) %>

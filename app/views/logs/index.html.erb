<p id="notice"><%= notice %></p>

<h1>履歴</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @logs.each do |log| %>
      <tr>
      <td><%= log.id %></td>
      <td><%= log.parts.name %></td>
      <td><%= log.created_at.strftime("%Y年%-m月%-d日 %-H時%-M分") %></td>
      <td><%=  %></td>
        <td><%= link_to 'Show', car_log_path(@car, log) %></td>
        <td><%= link_to 'Edit', edit_car_log_path(@car, log) %></td>
        <td><%= link_to 'Destroy', car_log_path(@car, log), method: :delete %></td>
      </tr>
    <% end %>
    <% @part.each do |part| %>
      <tr>
      <td><%= part.id %></td>
      <td><%= part.name %></td>
      <td><%=  %></td></tr>
      <% end %>
  </tbody>
</table>

<br>

<%= link_to '履歴作成', new_car_log_path(@car) %>